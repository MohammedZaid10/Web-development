Lesson 54 - Introduction to JS and Installing Node.JS

Node.js was created to see the output of the web browser runtime by the creator - read more about how and why node.js was created

npm is the package manager in JavaScript

Read code to know syntax of alert, console.log, var (variable), prompt, confirm and basic if statement

Not all commands that we run in js can be run using node.js like alerts, prompt, confirm as they can be only seen using a browser

we can add titles and change background color which will become inline css using JavaScript itself without making change to html or css file - see code

==================================================================================

Lesson 55 - JavaScript Variables, Data Types & Objects

a. Javascript variables are case-sensitive
b. var is globally scoped, let and const are locally scoped
c. we should let instead of var in most of the cases
d. when a variable is defined as const, it cannot be changed
f. Primitive data types:
    let x = 93;
    let y = "Zaid";
    let z = 3.55;
    let p = true;
    let q = undefined;
    let r = null;

    console.log(x,y,z,p,q,r)
    console.log(typeof x,typeof y,typeof z,typeof p,typeof q,typeof r)
    O/P : 93 'Zaid' 3.55 true undefined null
    O/P : number string number boolean undefined object
e. Objects:
    let o = {
    "name" : "Zaid",
    "job code" : 5609,
    "is_handsome" : true
    }

    console.log(o);
    o.salary = '100crores';
    console.log(o);
    o.salary = '500crores';
    console.log(o);

    O/P: 
    {name: 'Zaid', job code: 5609, is_handsome: true}
    {name: 'Zaid', job code: 5609, is_handsome: true, salary: '100crores'}
    {name: 'Zaid', job code: 5609, is_handsome: true, salary: '500crores'}

f. read handwritten notes for this lesson 55 as there are extra points not thoought in the video
g. practise all the challenges given in the handwritten notes
h. read all tutorial documentations

=============================================================================

Lesson 56 : JavaScript Conditionals : if, else if, else ladder

1. if else syntax: 
    let age = 5;

    if (age>18){
        console.log("you can drive")
    }

    else {
        console.log("you cannot drive")
    }

2. Arithmetic operator assignment operator, comparison operator, logical opertor and ternary operator

====================================================================

Lesson 57 - JS loops

1. for loop:

let a = 1;

for (let a = 0; a < 100; a++) 
    {
    console.log(a);
    }

2. forin loop:

let obj = {
    'name' : 'Zaid',
    'Age' : 25
}

for (const key in obj) {
        const element = obj[key];
        console.log(key,element)
    }

3. forof loop

for (const ha of 'harry') {
    console.log(ha)
}

4. while loop:
let i =7;

while (i < 10) {
    console.log(i)
    i++;
}

5. do-while loop:

when we want a loop to run atleast once:

let i = 7;

do{
    console.log(i);
    i++;
}while(i > 10)

====================================================================

Lesson 58 - JavaScript Functions

Read code which also has output with it

====================================================================

Lesson 60 - JavaScript Strings

Read code which also has output with it

====================================================================

Lesson 63 - JavaScript Arrays

a. JS arrays are mutable
b. arr.length
c. typeof arr (o/p : object)
d. arr[0] = 356; / to change an element in an array using its index
e. console.log(arr.toString()) /  to change an array of int type to string type
f. console.log(arr.join(" and ")) (o/p : 5666 and 2 and 3 and 4 and 5)
g. a.pop() - to remove last element
h. a.push('harry', 100) - to add harry and 700 and end of array
i.  a.shift() - to remove first element from an array
j. a.unshift('jack') - to add jack at starting of the array
k. delete a[4] - 4th element will get deleted but array lenghth will be same and memory allocation remains
l. concat (does not chnage the existing array):
    let a1 = [1,2,3]
    let a2 = [4,5,6]
    let a3 = [7,8,9]
    let a4 = a1.concat(a2,a3)
m. a.sort() - this will sort the array (also sort can take a comparison operator, read and experiment this from notes of harry)
n. let a=[1,2,3,4,5,6,7]
    a.splice(1,4, 233,333,433) - 1 is the position from which number should getting deleted from and 4 is the number of elements from the given position that should be deleted and then we are just adding 3 elements 233, 333,433 but we can add as manay as elements that we want
    console.log(a)
    o/p: [1,6,7]
o.  let a= [1,2,3,4,5]
    a.slice(2) / starting position
    o/p : [3,4,5]
    let a= [1,2,3,4,5]
    b = a.slice(1,3) / start & end position
    console.log(b)
    console.log(a)
    o/p: b = [1,4,5]
    a =[1,2,3,4,5]
    - slicing does not remove the elements, it just helps with extracting the required elements from the array, it creates a new array
p. a.reverse()

Looping over arrays
q. classic for loop
r. forEach loop
s. forin loop (only for json objects, not for arrays)
t. forof loop (created for arrays)
u. map() - see code of mfr.js
v. filter() - see code of mfr.js
w. reduce() - see code of mfr.js, best use case for factorial
x. Array.from("Harry")
    o/p:
    (5) ['H', 'a', 'r', 'r', 'y']

====================================================================

Lesson 66 - JavaScript DOM

Document Object Model - helps with mapping the entire document in the object where you get all the content of the page

Window contains:
DOM
BOM ($0)
Javascript Core

Read this later:
https://developer.mozilla.org/en-US/docs/Web/API/Document_Object_Model

====================================================================

Lesson 67 - JavaScript DOM - Child, parent & Sibling nodes

document.body
document.body.childNodes[0]
document.body.childNodes[1]
document.body.childNodes[1].childNodes:
NodeList(11) [text, div.box, text, div.box, text, div.box, text, div.box, text, div.box, text]
- we get text also because these are the spaces that are there between each syntax

let chn = document.body.childNodes[1]
chn - just give the variable and press enter to see the result stored in Variables
chn.firstChild - this will give you the first text field
chn.firstElementChild - this will give you the first field which has a meaningful element like a div or span
chn.lastElementChild
chn.lastElementChild.style.color = "red";
chn.lastElementChild.style.backgroundColor = "green";
document.body.firstElementChild.children:
HTMLCollection(5) [div.box, div.box, div.box, div.box, div.box]
document.body.firstElementChild.children[1]
document.body.firstElementChild.children[0].nextElementSibling :
<div class=​"box">​Box 2​</div>​
document.body.firstElementChild.children[1].previousElementSibling :
<div class=​"box">​Box 1​</div>​
document.body.firstElementChild.children[0].previousSibling: 
#text
document.body.firstElementChild.children[0].parentElement: 
<div class=​"container">​…​</div>​
tables 9read Harry's notes):
document.body.children[1].rows :
HTMLCollection(2) [tr, tr]

====================================================================

Lesson 68 - JavaScript Selecting by IDs, Classes and More



















